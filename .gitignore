# Réglages locaux Claude Code (CLAUDE.md, lui, est versionné : les sessions cloud partent
# d'un clone neuf et ne liraient pas une copie locale)
.claude/

# macOS
.DS_Store

# Python (serveur de démo)
__pycache__/
*.pyc

# Artefacts de build
build/
SNCFWifi.app/
/tmp/
